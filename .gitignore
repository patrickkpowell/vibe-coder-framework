# Projects (each project is its own git repo)
project-*/
# Specific project description files (not template)
Project-[0-9]*.md

# Claude Code local settings and runtime state (machine-specific)
.claude/settings.local.json
.claude/worktrees/

# OS
.DS_Store
Thumbs.db

# Secrets
.env
*.key
*.pem
credentials.*
secrets.*
